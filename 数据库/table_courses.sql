create table courses (courseId int,
                        courseName varchar(20),
                        teaName varchar(20), -- the teacher's name
                        hwNum int,
                        primary key(courseId));